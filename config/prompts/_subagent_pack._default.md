You are processing {N} independent tasks in BATCH MODE.
Return a single JSON array (no markdown fences) of length {N},
where element i is the result for task i.

Tasks:
{TASKS_JSON}

Important:
- Respond with ONLY the JSON array, no additional text or explanation.
- The array must have exactly {N} elements.
- Each element should be a JSON object with the result for the corresponding task.
